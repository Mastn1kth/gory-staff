# Горы: полный состав проекта

Этот файл нужен как понятное описание того, что уже есть в проекте приложения «Горы».

## Главная схема

- Компьютер владельца работает как сервер ресторана.
- На компьютере запускаются backend, PostgreSQL и локальный HTTPS relay к Cloudflare.
- Телефон гостя или сотрудника устанавливает Android APK.
- APK подключается к серверу через публичный HTTPS-адрес или локальный Wi-Fi.
- Основной адрес: `https://app.gory-staff.ru`.
- Данные хранятся на сервере в PostgreSQL.
- Телефон хранит кэш последних данных и очередь офлайн-действий.

## Батники

- `START_GORY_STAFF.bat` запускает Docker, PostgreSQL, backend, публичный HTTPS relay, iiko event connector и проверяет адреса.
- `STOP_GORY_STAFF.bat` останавливает сервер, PostgreSQL-контейнер, публичный relay, iiko event connector, Metro/Expo и Gradle daemons.
- `START_PUBLIC_RELAY.bat` запускает исходящую связь компьютера ресторана с Cloudflare для домена `https://app.gory-staff.ru`.
- `START_IIKO_EVENT_CONNECTOR.bat` запускает локальный iiko event connector в watch-режиме: события читаются из `runtime\iiko\events`, секрет берется из `server\.env`, логи пишутся в `runtime\logs`.
- `BUILD_ANDROID_APK.bat` собирает свежий Android APK локально через Android Studio / Gradle.

## Android APK

- Готовый файл: `Gory-latest.apk`.
- Название приложения на телефоне: «Горы».
- Основной сервер внутри APK: `https://app.gory-staff.ru`.
- Для Wi-Fi теста можно собрать APK с локальным адресом через `BUILD_ANDROID_APK.bat wifi`.
- `localhost` и `127.0.0.1` не используются для реального телефона.

## Гостевая часть

- Заставка «Горы».
- 4 гостевые вкладки: Профиль, Главная, Меню, Как добраться.
- Свайп между вкладками.
- Атмосферная главная страница ресторана.
- Гостевое меню без корзины, доставки, заказов и оплаты.
- Отдельный раздел «Как добраться».
- Кнопки маршрута, звонка и копирования адреса.
- Гостевой профиль.
- Регистрация гостя по имени и телефону.
- Вход гостя по телефону.
- OAuth-вход гостя через mobile deep link `gory-staff://oauth/{provider}`.
- Бонусная карта.
- Баланс бонусов.
- История бонусов.
- Реферальный код.
- Настоящий QR для реферального кода и временного кода списания бонусов.
- Реферальная регистрация.
- Кэш гостевого профиля, меню и бонусов.

## Клиентская база

- Гости отделены от сотрудников.
- Есть таблицы гостей, карт, бонусных операций, рефералов, сессий, устройств, согласий, заметок и сегментов.
- У гостя есть телефон, бонусы, уровень лояльности и реферальный код.
- Бонусный баланс считается через операции, а не простым ручным числом.
- Предусмотрены уровни: бронза, серебро, золото, платина.
- При регистрации начисляется стартовый бонус.
- Реферальный бонус защищён от повторного начисления.

## Рабочая зона персонала

- Рабочая зона скрыта внутри гостевого профиля через кнопку «Для сотрудников».
- Доступ управляющего задаётся через `INITIAL_MANAGER_LOGIN` и `INITIAL_MANAGER_PASSWORD` в `server\.env`; демо-пароли не показываются в приложении.
- Роли и права доступа сохранены.
- Управляющий видит админские разделы.
- Официант видит рабочие разделы официанта.
- Хостес видит план зала и брони.
- Кухня и бар видят свои рабочие разделы.
- Чат из базы не удалён, но в интерфейсе не показывается.

## Рабочие модули

- Главная смены.
- План зала.
- Два этажа.
- Столики с разными статусами.
- Изменение статуса столиков.
- Брони.
- Создание и редактирование броней.
- Меню.
- Стоп-лист.
- График.
- Персонал.
- Банкеты и мероприятия.
- Новости и уведомления.
- Правила / информация.
- Задачи.
- Профиль сотрудника.
- Админ-раздел.
- Аналитика.
- Клиенты для управляющего.
- Блокнот официанта за смену.
- Лист ожидания.
- Заявки кухни и бара.
- Предоплаты и прозвон по банкетам.
- История действий.

## Онлайн через мобильный интернет

- Поддержан публичный адрес `https://app.gory-staff.ru` через Cloudflare Worker и локальный HTTPS relay.
- APK должен работать через мобильный интернет, если ПК-сервер и локальный relay запущены.
- Обычный Cloudflare Tunnel на текущем интернет-канале не используется, потому что его постоянное соединение обрывалось при проверках.
- В батнике показываются адреса для ПК, Wi-Fi и мобильного интернета.
- В приложении есть диагностика API URL, WebSocket, сервера и push.

## Push-уведомления

- Добавлена база под push-устройства.
- Есть таблицы уведомлений, настроек и delivery log.
- Телефон может получить push token.
- Token привязывается к гостю или сотруднику.
- Есть тестовое push-уведомление.
- Секретные ключи Firebase не хардкодятся.

## SMS-напоминания

- Добавлен сервис напоминаний о бронях.
- Twilio status callback принимает form payload.
- Twilio webhook проверяет `X-Twilio-Signature`, если задан `TWILIO_AUTH_TOKEN`.
- Для production нужен точный `TWILIO_STATUS_CALLBACK_URL`, совпадающий с публичным callback URL.

## Offline-first

- Приложение открывается без интернета, если данные уже загружались.
- Сессия гостя не сбрасывается при потере сети.
- Сессия сотрудника не сбрасывается при потере сети.
- Меню, профиль, бонусы, рабочие данные и план зала показываются из кэша.
- Без интернета не показывается пустой экран.
- Есть статус «Нет соединения».
- Есть автоматическое восстановление подключения.
- Есть очередь офлайн-действий.
- Разрешённые офлайн-действия сохраняются и отправляются позже.
- Критичные действия без сервера блокируются.

## Что нельзя делать без интернета

- Новый вход, если пользователь раньше не входил на телефоне.
- Регистрация гостя.
- Начисление и списание бонусов.
- Изменение ролей.
- Блокировка сотрудников.
- Удаление данных.
- Критичные админские действия.
- Push-отправка.

## Что можно делать без интернета

- Смотреть гостевой профиль.
- Смотреть бонусную карту.
- Смотреть меню.
- Смотреть адрес и маршрутную информацию.
- Смотреть рабочую зону из кэша.
- Смотреть план зала, брони, задачи, стоп-лист.
- Делать записи в блокнот официанта.
- Ставить часть рабочих изменений в очередь.

## Сервер

- Node.js backend.
- PostgreSQL через Docker.
- Socket.IO realtime.
- Авторизация сотрудников.
- Гостевая авторизация.
- API для гостей отделён от API сотрудников.
- Синхронизация данных.
- История действий.
- Push-сервис.

## База данных

- PostgreSQL хранит основные данные.
- Есть таблицы сотрудников, ролей, смен, меню, стоп-листа, этажей, столов, броней, событий, объявлений, правил, задач, чатов, сообщений, истории действий.
- Есть гостевые таблицы.
- Есть push-таблицы.
- Данные сохраняются после выключения сервера.

## Что специально не добавлено

- Корзина.
- Доставка.
- Онлайн-заказ.
- Оплата.
- Apple Pay / Google Pay.
- Интеграция с кассой.
- r_keeper.

## Интеграция с iikoCloud

- Добавлена синхронизация с iikoCloud: ручная синхронизация категорий, блюд, цен, стоп-листа, данных модификаторов и персонала в PostgreSQL, отправка гостевых заказов в iiko table orders и ручное подтягивание статуса конкретного iiko-заказа обратно в локальную БД.
- Настройка только через `server\.env`: обязательные `IIKO_ENABLED`, `IIKO_API_LOGIN`, `IIKO_ORGANIZATION_ID`; для order sync также нужен `IIKO_TERMINAL_GROUP_ID`; опциональные `IIKO_API_BASE`, `IIKO_ORDER_SYNC_ENABLED`, `IIKO_ORDER_STATUS_SYNC_ENABLED`, `IIKO_ORDER_STATUS_SYNC_INTERVAL_SECONDS`, `IIKO_ORDER_STATUS_SYNC_LIMIT`, `IIKO_SOURCE_KEY`, `IIKO_SERVICE_PRINT`, `IIKO_CHECK_STOP_LIST`, `IIKO_TRANSPORT_TIMEOUT_SECONDS`, `IIKO_WEBHOOK_SECRET`.
- Синхронизация выключена, если `IIKO_ENABLED` не равен `true` или не задан `IIKO_API_LOGIN`; без `IIKO_ORGANIZATION_ID` sync не может успешно загрузить меню, без `IIKO_TERMINAL_GROUP_ID` не может отправлять заказы.
- Данные iiko хранятся через внешние `iiko_id` в `menu_categories`, `menu_items`, `users` и служебные поля стоп-листа.
- Повторный запуск sync обновляет существующие записи по `iiko_id`, а не создает дубли.
- Блюда, пропавшие из ответа iiko, получают `status = 'archived'`, но не удаляются.
- Сотрудники, пропавшие из ответа iiko, получают `status = 'archived'` и `iiko_is_deleted = TRUE`, но не удаляются.
- iiko-модификаторы хранятся отдельно от обычного меню в `menu_item_modifier_groups` и `menu_item_modifiers`; группы/позиции, исчезнувшие из nomenclature, получают `status = 'archived'`.
- Локальные поля меню, которых нет в iiko, не затираются: подсказки официанту, рекомендации, себестоимость и ручные служебные поля остаются локальными.
- Доступны admin endpoints `GET /iiko/status`, `POST /iiko/sync/menu`, `POST /iiko/sync/orders/:orderId`, `POST /iiko/sync/orders/:orderId/status` и `POST /iiko/sync/orders/statuses`; нужна staff-авторизация и доступ `manage:menu`.
- `GET /iiko/status` показывает расширенную диагностику: включение интеграции, обязательные env, отсутствующие env, замаскированный `IIKO_API_LOGIN`, organization/terminal group, последний menu sync, последний order sync, счетчики категорий/позиций/заказных позиций и последнюю ошибку.
- Результаты menu sync пишутся в `iiko_sync_log`; результаты order sync пишутся в `iiko_order_sync_log`; успешный sync в ручном endpoint остается `completed`, а в диагностике `/iiko/status` отображается как `success`.
- При гостевом заказе сервер создает заказ в iiko через `/api/1/order/create`, сохраняет `guest_orders.iiko_order_id`, а следующие новые позиции отправляет через `/api/1/order/add_items`; состояние позиций хранится в `guest_order_items.iiko_position_id`, `iiko_sync_status`, `iiko_sync_error`, `iiko_synced_at`.
- `POST /iiko/sync/orders/:orderId/status` вызывает `/api/1/order/by_id`, сохраняет `guest_orders.iiko_order_status`, `iiko_order_number`, `iiko_order_sum`, `iiko_order_closed_at`, `iiko_order_payload_json`; при статусе iiko `Closed` локальный заказ закрывается и активная гостевая сессия завершается.
- При старте сервера включается фоновый pull статусов открытых iiko-заказов, если iiko/order sync включены. По умолчанию интервал 60 секунд, минимум 30 секунд, лимит одного прохода 50 заказов; отключается через `IIKO_ORDER_STATUS_SYNC_ENABLED=false`.
- Добавлены документы для ручной проверки и дальнейшего структурного наращивания: `docs/IIKO_MANUAL_CHECKLIST.md`, `docs/IIKO_ORDER_SYNC_CHECKLIST.md`, `docs/IIKO_READONLY_ROADMAP.md`, `docs/IIKO_CHECK_REPORT_TEMPLATE.md`, `docs/IIKO_TROUBLESHOOTING.md`.
- Добавлен защищенный endpoint `POST /iiko/events/payment-paid` для локального iikoFront/edge-коннектора: принимает факт уже прошедшей оплаты, сопоставляет гостя, закрывает локальную гостевую сессию/заказ и отправляет push-запрос оценки визита.
- Добавлено списание бонусов к оплачиваемому iiko-заказу: `1` балл = `1` рубль, максимум `20%` суммы заказа, обязательная связь через `iiko_order_id` или `local_order_id`, применение по payment-paid webhook и отображение в разделе клиентов.
- Добавлен прием `POST /iiko/events/order-updated`: локальный iikoFront/edge-коннектор может заранее прислать открытый iiko-заказ, сервер сохраняет его в `iiko_external_orders`, связывает с активным гостем по столу/телефону/session и затем применяет `payment-paid` даже если в оплате пришли только `order_id` и сумма.
- Добавлен `tools/iiko-event-connector.js`: локальный мост читает JSON/JSONL события из файла, папки или stdin, умеет постоянный `--watch` папки `runtime\iiko\events`, загружает секрет из `server\.env`, отправляет `order-updated`/`payment-paid` в сервер с `X-Gory-Iiko-Secret` и ведет state-файл против повторной отправки.
- Основной запуск сохраняет существующие `IIKO_*` переменные в `server\.env`, стартует iiko connector после старта API, stop-скрипт его останавливает, watchdog перезапускает, а панель управления показывает состояние процесса.
- Не реализовано: native iikoFront plugin внутри кассового терминала, live-проверка на реальной iiko-точке, проведение оплаты в приложении, касса, фискализация, изменение оплат и закрытие заказа в iiko из приложения, UI выбора модификаторов в заказе.
- Веб-версия.
- PWA.
- Чат в интерфейсе.

## Что ещё осталось улучшить

- Финальная серверная защита конфликтов по `updated_at` / version.
- Полная FCM-конфигурация для production push.
- Настоящее меню ресторана вместо тестового.
- Настоящий план рассадки вместо тестового.
- Финальная безопасность production: домен, HTTPS без туннельной зависимости, резервные копии, нормальные пароли.
- iOS-сборка через TestFlight.

## Последний большой UX-проход

- Гостевая часть уже работает в 4 вкладках: Профиль, Главная, Меню, Маршрут.
- Реферальный код и временный код списания бонусов открываются как карточки с настоящим QR-кодом, копированием/обновлением и понятным текстовым кодом.
- Уровень лояльности открывает отдельное окно с уровнями и преимуществами.
- Тестовый push больше не является обычным блоком гостевого профиля; проверка push остаётся в диагностике.
- Вход сотрудников получил быстрые dev-кнопки и форму, которая поднимается над клавиатурой.
- Главный рабочий экран управляющего стал чище: пульс смены без лишней карточки имени.
- Раздел клиентов очищен от общего блока последних операций; история бонусов показывается при работе с выбранным клиентом.
- У сотрудника есть редактирование профиля и выбор аватарки-заглушки.
- У официанта появился блок `Акценты продаж` в меню.
- У хостес есть рабочие блоки `Сегодня`, `Зал`, `Брони` и `Ожидание`.
- Сервер создаёт уведомления по бонусам, рефералам, броням, листу ожидания, стоп-листу и задачам.
- Корневая папка визуально очищена: на виду только батники, README и актуальный APK.
